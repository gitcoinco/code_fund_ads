<%= render "/shared/index_layout",
    title: "Organizations",
    buttons: [
      link_to("Add organization", new_organization_path, class: "btn btn-primary")
    ] do %>
  <%= render "/shared/data_list",
              resource_name: "organizations",
              scopable: %w[positive negative zero],
              collection: @organizations,
              pagy: @pagy %>
<% end %>
