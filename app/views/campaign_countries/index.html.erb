<%= render layout: "/shared/layout", locals: {
  subject: @campaign,
  sidebar: true,
  datepicker: true,
  buttons: [
    layout_button(link: new_campaign_path(clone: @campaign.id), icon: "fad fa-copy", title: "Clone campaign")
  ]
} do %>
  <%= card do %>
    <%= card_body padding: "p-0" do %>
      <%= render "/campaign_countries/table", campaign: @campaign %>
    <% end %>
  <% end %>
<% end %>
