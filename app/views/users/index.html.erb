<%= render "/shared/index_layout",
    title: "Users",
    buttons: [
      link_to("Invite User", new_user_invitation_path, class: "btn btn-primary")
    ] do %>
  <%= render "/shared/data_list",
             resource_name: "users",
             scopable: %w[administrators advertisers publishers],
             collection: @users,
             pagy: @pagy %>
<% end %>
