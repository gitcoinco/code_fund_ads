<%= render layout: "/organizations/show_layout", locals: { organization: @eventable } do %>
  <h3 class="h5 mb-3">
    Events
    <small class="text-muted d-block">List of events related to this organization</small>
  </h3>
  <%= render "/events/table", eventable: @eventable, events: @events %>
<% end %>
