<%= render(PageComponent.new(subject: @user)) do |component| %>
  <%= component.with(:header) { render(Page::HeaderComponent.new(title: "Edit Password", subtitle: "Fill out the form below")) } %>
  <%= component.with(:body) { render("/user_passwords/form") } %>
<% end %>
