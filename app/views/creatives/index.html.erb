<%= render(PageComponent.new) do |component| %>
  <% component.with(:header) do %>
    <%= render(Page::HeaderComponent.new(title: "Creatives", buttons: [
      layout_button(link: new_creative_path, icon: "fas fa-plus", title: "Add creative")
    ])) %>
  <% end %>
  <% component.with(:body) do %>
    <%= render "/shared/data_list", resource_name: "creatives", collection: @creatives, pagy: @pagy %>
  <% end %>
<% end %>
