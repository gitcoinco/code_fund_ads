import './theme.scss'
export default {
  mustache: '<%= File.read(Rails.root.join("app/javascript/advertisements/square/template.html.mustache"), encoding: "UTF-8").squish %>',
  initialize: ad => {
    if (ad.visible) ad.trackImpression()
  }
}
