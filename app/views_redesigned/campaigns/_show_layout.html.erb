<% render 'campaigns/actions', campaign: campaign %>
<% render 'campaigns/page_heading', campaign: campaign %>

<div class="d-flex">
  <%= render "shared/sidebar" %>
  <div class="container mt-4">
    <header class="page-title-bar">
      <%= yield :page_heading %>
      <div class="d-flex border-bottom">
        <%= render "campaigns/tabs", campaign: campaign %>
        <%= yield :actions %>
      </div>
    </header>
    <%= yield %>
  </div>
</div>
