<%= render "/shared/index_layout",
    title: "Campaigns",
    buttons: [
      link_to("Add campaign", new_campaign_path, class: "btn btn-primary")
    ] do %>
  <%= render "/shared/data_list",
               resource_name: "campaigns",
               scopable: %w[pending active archived],
               collection: @campaigns,
               pagy: @pagy %>
<% end %>
