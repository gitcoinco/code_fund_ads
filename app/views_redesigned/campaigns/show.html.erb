<div class="container mt-4">
  <div class="row">
    <div class="col-4">
      <% if @campaign.standard? %>
        <%= render "campaigns/standard_card", campaign: @campaign %>
      <% end %>
    </div>
  </div>
</div>
