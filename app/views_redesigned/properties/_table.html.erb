<div class="table-responsive">
  <table class="js-datatable table table-striped">
    <thead>
      <tr>
        <th>&nbsp;</th>
        <%= sortable_tr "name" %>
        <%= tag.th("Publisher", scope: "col") if authorized_user.can_admin_system? %>
        <th scope="col">Est. Traffic</th>
        <%= sortable_tr "created_at", "Date Added" %>
        <%= sortable_tr "status" %>
        <th scope="col">Impressions</th>
        <th scope="col">CTR</th>
        <th scope="col"></th>
      </tr>
    </thead>
    <tbody>
      <%= render partial: "/properties/tr", collection: properties, as: :property %>
    </tbody>
  </table>
  <div class="d-flex justify-content-between align-items-center">
    <span><%== pagy_bootstrap_nav(pagy) if pagy.pages > 1 %></span>
    <%= pagy_entries(pagy) %>
  </div>
</div>
