<%= render "/shared/index_layout",
    title: "Properties",
    buttons: [
      link_to("Add property", new_property_path, class: "btn btn-primary")
    ] do %>
  <%= render "/shared/data_list",
             resource_name: "properties",
             scopable: %w[active archived blacklisted pending rejected],
             collection: @properties,
             pagy: @pagy %>
<% end %>
